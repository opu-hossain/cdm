# Release metadata for automated package generation.
# Allowed channels: "rc" or "stable".
# "rc" creates version strings like 0.1.0-rc1, and "stable" creates 0.1.0.
# Changes here do not trigger a release. Push a matching vX.Y.Z[-rcN] tag
# (for example with scripts/cdm tag), or manually dispatch a release workflow.

channel = "rc"
version = "0.2.0"
rc = 1
publish = true
